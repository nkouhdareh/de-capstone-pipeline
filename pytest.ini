[pytest]
# The repo root must be importable so `from rag.corpus import chunker` resolves.
# Bare `pytest` does not add the working directory to sys.path (only
# `python -m pytest` does), which passed locally and failed in CI.
pythonpath = .
testpaths = tests rag/tests
